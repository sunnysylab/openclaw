---
phase: 03-outbound-surface-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infra/net/outbound-surfaces.test.ts
  - docs/reference/outbound-surfaces.md
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "A catalog document exists listing all outbound HTTP surfaces grouped by category"
    - "Each surface is annotated Yes/No for SSRF chokepoint coverage"
    - "A spot-check test proves the web fetch tool path triggers DnsBlocklistError for blocked domains"
    - "Agent-controlled URLs are annotated distinctly from operator-configured ones"
  artifacts:
    - path: "docs/reference/outbound-surfaces.md"
      provides: "Outbound HTTP surface catalog"
      contains: "Agent Tools"
    - path: "src/infra/net/outbound-surfaces.test.ts"
      provides: "Spot-check test proving blocklist works through fetch-guard"
      contains: "DnsBlocklistError"
  key_links:
    - from: "src/infra/net/outbound-surfaces.test.ts"
      to: "src/infra/net/fetch-guard.ts"
      via: "import fetchWithSsrFGuard and assert DnsBlocklistError"
      pattern: "fetchWithSsrFGuard.*malware\\.test"
---

<objective>
Catalog all gateway outbound HTTP surfaces and prove chokepoint coverage with a spot-check test.

Purpose: Complete the DNS blocklist feature by documenting which outbound paths are protected by the SSRF chokepoint and which are not, satisfying TEST-03.
Output: A Markdown catalog at `docs/reference/outbound-surfaces.md` and a spot-check test at `src/infra/net/outbound-surfaces.test.ts`.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-outbound-surface-catalog/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/infra/net/fetch-guard.ts:

```typescript
export type GuardedFetchOptions = {
  url: string;
  fetchImpl?: FetchLike;
  init?: RequestInit;
  maxRedirects?: number;
  timeoutMs?: number;
  signal?: AbortSignal;
  policy?: SsrFPolicy;
  lookupFn?: LookupFn;
  // ...
};

export async function fetchWithSsrFGuard(params: GuardedFetchOptions): Promise<GuardedFetchResult>;
```

From src/infra/net/domain-filter.ts:

```typescript
export class DnsBlocklistError extends SsrFBlockedError { ... }
export function isDomainBlocked(hostname: string): boolean;
```

From src/infra/net/ssrf.ts:

```typescript
export type LookupFn = typeof dnsLookup;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spot-check test proving web fetch path triggers DnsBlocklistError</name>
  <files>src/infra/net/outbound-surfaces.test.ts</files>
  <behavior>
    - Test 1: Calling fetchWithSsrFGuard with url "https://malware.test/path" and a public-IP DNS mock rejects with DnsBlocklistError
    - Test 2: The DNS lookup mock is never called (blocklist fires before DNS resolution)
  </behavior>
  <action>
Create `src/infra/net/outbound-surfaces.test.ts` following the pattern from `ssrf.pinning.test.ts`.

Import `DnsBlocklistError` from `./domain-filter.js`, `fetchWithSsrFGuard` from `./fetch-guard.js`, and `LookupFn` type from `./ssrf.js`.

Create a `createPublicLookupMock()` helper that returns `vi.fn(async () => [{ address: "93.184.216.34", family: 4 }])` cast as `LookupFn`.

Write a describe block "outbound surface catalog spot-check" with one test: "web fetch tool path rejects blocked domain with DnsBlocklistError". The test calls `fetchWithSsrFGuard({ url: "https://malware.test/path", lookupFn: lookup })` and asserts it rejects with `DnsBlocklistError`. Also assert `lookup` was not called (proving pre-DNS rejection).

Run RED first (confirm test infrastructure works), then the test should pass GREEN immediately since the blocklist guard was wired in Phase 2.
</action>
<verify>
<automated>cd /home/john/vault/projects/github.com/openclaw && pnpm test src/infra/net/outbound-surfaces.test.ts</automated>
</verify>
<done>Test file exists, test passes, proving fetchWithSsrFGuard rejects malware.test with DnsBlocklistError before DNS lookup</done>
</task>

<task type="auto">
  <name>Task 2: Write outbound HTTP surface catalog document</name>
  <files>docs/reference/outbound-surfaces.md</files>
  <action>
Create `docs/reference/outbound-surfaces.md` with the following structure. Use the complete surface inventory from the RESEARCH.md (03-RESEARCH.md has the full trace). The catalog is a factual audit -- no recommendations, no TODOs, no GitHub issues.

**Title:** "Outbound HTTP Surfaces"

**Intro paragraph:** Brief description of what this catalog is (audit of all gateway outbound HTTP call sites), what "Guarded" means (flows through `fetchWithSsrFGuard` SSRF chokepoint), and the date of the audit.

**Table columns per user decision:** Surface | Source | Guarded | Notes

**Categories (each gets its own H2 and table):**

1. **Agent Tools** -- highest-risk, agent-controlled URLs. Include: Web Fetch (Yes, SSRF chokepoint, agent-controlled URL), Web Search redirect follow (Yes), Skills download (Yes), Firecrawl fallback (No, hardcoded Firecrawl API endpoint, agent-controlled URL in request body).

2. **Channel APIs** -- vendor-fixed endpoints. Include: Telegram Bot API (No, grammy SDK, hardcoded api.telegram.org), Discord REST API (No, bare fetch, hardcoded Discord API), Slack Web API (No, SDK-managed, hardcoded Slack API), Slack file upload (Yes, SSRF chokepoint via withTrustedEnvProxyGuardedFetchMode), Signal (No outbound HTTP -- local CLI subprocess), iMessage (No outbound HTTP -- local RPC subprocess). For Signal and iMessage, mark Guarded as "Yes" with Notes "No outbound HTTP" per binary Yes/No rule -- OR mark "No" with note "No outbound HTTP (local subprocess)". Use discretion for the clearest representation. Per user decision, binary Yes/No only.

3. **Provider APIs** -- operator-configured endpoints. Include all from RESEARCH.md: Ollama, TTS providers, Vercel AI Gateway, Venice models, HuggingFace models, Kilocode models, OpenCode Zen models, Minimax VLM, PDF native providers, Bedrock discovery.

4. **Media Pipeline** -- Include: Media fetch input files (Yes), Media fetch general (Yes), Media understanding (Yes), Camera node fetch (Yes).

5. **Infrastructure** -- Include: Cron webhook delivery (Yes), Remote memory HTTP (Yes), Gateway probe (No, localhost probe), Browser sandbox (No, localhost).

6. **Extensions (Sample)** -- 3-5 representative extensions per user decision. Include from RESEARCH.md: Matrix client config (Yes, plugin-sdk fetchWithSsrFGuard), Matrix directory (No, bare fetch to operator homeserver), MS Teams Graph API (No, bare fetch to hardcoded MS Graph), MS Teams attachments (Yes, plugin-sdk fetchWithSsrFGuard), Feishu streaming cards (Yes, plugin-sdk fetchWithSsrFGuard). Add a note that the plugin SDK re-exports `fetchWithSsrFGuard` so extensions CAN use it.

**Source column:** Use backtick-formatted repo-relative file paths (e.g. `src/agents/tools/web-fetch.ts`).

**Agent-controlled URL annotation:** Per user decision, flag agent-controlled URLs distinctly in the Notes column. For Agent Tools entries where the AI picks the URL, add "(agent-controlled URL)" to Notes.

**Keep tables clean for Mintlify:** Short cells, backtick code paths, no pipes in content.

Do NOT add a mintlify nav entry -- just create the document.
</action>
<verify>
<automated>cd /home/john/vault/projects/github.com/openclaw && test -f docs/reference/outbound-surfaces.md && grep -c "Agent Tools" docs/reference/outbound-surfaces.md && grep -c "| Yes" docs/reference/outbound-surfaces.md</automated>
</verify>
<done>Catalog document exists at docs/reference/outbound-surfaces.md with all 6 category tables, binary Yes/No Guarded column, agent-controlled URL annotations, and no recommendations or TODOs</done>
</task>

</tasks>

<verification>
1. `pnpm test src/infra/net/outbound-surfaces.test.ts` passes
2. `docs/reference/outbound-surfaces.md` exists with all 6 categories
3. Each surface has binary Yes/No in Guarded column
4. Agent-controlled URLs are annotated distinctly
5. No recommendations, TODOs, or GitHub issues in the catalog
6. `pnpm test` full suite passes (no regressions)
</verification>

<success_criteria>

- Spot-check test proves fetchWithSsrFGuard rejects blocked domains with DnsBlocklistError
- Catalog lists all outbound HTTP surfaces from RESEARCH.md inventory
- Categories: Agent Tools, Channel APIs, Provider APIs, Media Pipeline, Infrastructure, Extensions (Sample)
- Each entry: Surface, Source (file path), Guarded (Yes/No), Notes (guard type)
- TEST-03 requirement satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/03-outbound-surface-catalog/03-01-SUMMARY.md`
</output>
