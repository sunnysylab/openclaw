# 头号助手

> 最后更新：2026-03-28
> 状态：Phase 1-3 完成，Phase 4+ 规划中

## 核心价值

**AI 全方位接管你的信息流，你只做"想和决定"。**

不只是聊天助手——头号助手接管消息、日程、健康、通知、文件，让你从信息洪流中解放出来。AI 负责看、筛、找、转、记、提醒，你只需要做决策。

---

## 系统架构

### 设计原则

1. **OpenClaw 做中枢** — 连接所有渠道和数据源，统一出入口
2. **双模型分层调度** — 日常任务走 DeepSeek（低成本），复杂决策走 Claude CLI（Max 订阅零成本）
3. **MCP 扩展数据源** — 通过 MCP Server 接入本地数据（微信、健康、日历等）
4. **隐私第一** — 所有数据本地处理，不用于训练，不给第三方

### 架构图

```
        ┌─────────────────────────────────────────────────────┐
        │                    用户（手机/电脑）                    │
        └──┬──────┬──────┬──────┬──────┬──────┬───────────────┘
           │      │      │      │      │      │
        ┌──┴─┐ ┌──┴─┐ ┌──┴──┐ ┌─┴──┐ ┌─┴──┐ ┌┴────┐
        │微信│ │飞书│ │iMsg │ │邮箱│ │App │ │健康 │  ...
        └──┬─┘ └──┬─┘ └──┬──┘ └─┬──┘ └─┬──┘ └┬────┘
           │      │      │      │      │      │
        ┌──┴──────┴──────┴──────┴──────┴──────┴──────────────┐
        │               OpenClaw（中枢）                       │
        │                                                     │
        │  ┌─────────────┐  ┌──────────────────────────────┐  │
        │  │ 消息路由      │  │ Skill 系统                    │  │
        │  │ 分类/转发/忽略 │  │ inbox-triage / daily-digest  │  │
        │  │              │  │ file-ops / dev-proxy         │  │
        │  │              │  │ deep-research / memory       │  │
        │  └──────┬───────┘  └──────────────────────────────┘  │
        │         │                                            │
        │  ┌──────┴─────────────────────┐                      │
        │  │       模型路由层             │                      │
        │  │  DeepSeek ← 日常任务        │                      │
        │  │  Claude CLI ← 复杂任务      │                      │
        │  └────────────────────────────┘                      │
        │                                                     │
        │  ┌────────────────────────────┐                      │
        │  │     系统操作 / 文件 / Shell  │                      │
        │  └────────────────────────────┘                      │
        └──────────────┬──────────────────────────────────────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
    ┌─────┴──────┐ ┌──┴───────┐ ┌──┴──────────┐
    │ 微信 MCP   │ │ Gmail    │ │ 健康/日历    │  ...更多 MCP
    │ 本地DB解密  │ │ MCP      │ │ MCP         │
    └────────────┘ └──────────┘ └─────────────┘
```

### 模型分工策略

| 任务类型 | 模型 | 触发方式 | 示例 |
|----------|------|----------|------|
| 消息分类/摘要 | DeepSeek | 自动 | 标注紧急/待回复/已知/忽略 |
| 模板化回复 | DeepSeek | 自动 | "好的收到"、日程确认 |
| 文件操作 | DeepSeek | 用户指令 | "找一下上周的报价单" |
| 深度研究 | Claude | `@model claude-cli/opus` | 行业分析、技术调研 |
| 代码开发 | Claude | `@model claude-cli/opus` | 修 bug、写功能 |
| 健康分析 | Claude | 用户触发 | "这周运动量怎么样" |

---

## 数据源现状（2026-03-28）

### 消息渠道

| 通道 | 读 | 写 | 接入方式 | 状态 |
|------|----|----|----------|------|
| 飞书 | ✅ | ✅ | OpenClaw 官方插件 | 生产可用 |
| iMessage | ✅ | ✅ | BlueBubbles channel | 生产可用 |
| 微信（收发） | ✅ | ✅ | openclaw-weixin 插件 | 生产可用（仅1对1） |
| 微信（深度读取） | ✅ | - | MCP Server 本地 DB 解密 | 生产可用 |
| WhatsApp | ✅ | ✅ | OpenClaw 插件 | 已配置 |
| Discord | ✅ | ✅ | OpenClaw 插件 | 已配置（未启用） |
| 邮箱（Gmail） | ✅ | ✅ | 本地 Gmail MCP Server | **2026-03-28 接通** |

### 系统数据源（规划中）

| 数据源 | 读 | 写 | 接入方式 | 状态 |
|--------|----|----|----------|------|
| Google Calendar | ❌ | ❌ | claude.ai Calendar MCP | 待认证 |
| 剪贴板 | ❌ | ❌ | 自建 MCP（Python） | 待开发 |
| 健康数据(Garmin) | ✅ | - | Garmin Connect MCP Server | **2026-03-28 接通** |
| 手机通知 | ❌ | ❌ | KDE Connect / 转发 | 待调研 |
| 浏览器书签 | ❌ | ❌ | 本地文件读取 MCP | 待开发 |
| 快递追踪 | ❌ | ❌ | 快递100 API MCP | 待调研 |
| 财务账单 | ❌ | ❌ | 支付宝/微信账单导出 | 待调研 |

---

## 场景规划

### 已实现场景

#### 场景一：消息流转
三大渠道消息统一处理，自动分类，紧急消息跨渠道转发到 iMessage。
- 公众号/广告自动忽略
- 紧急消息转发到 iMessage
- 微信历史搜索（MCP）

#### 场景二：设备协同
手机发消息操作电脑文件。file-ops skill 已就绪。

#### 场景三：远程开发
手机指挥电脑写代码。dev-proxy skill + Claude CLI。

#### 场景四：深度学习
对任何主题深度研究。deep-research skill + Claude。

### 规划中场景

#### 场景五：日程管家

**目标**：接管日历，每天告诉你该做什么、什么时候出发。

**用户故事**：
- 早上推送："今天有3个会议，最早10点飞书群会。微信有2条待回复。"
- "明天下午3点在南山开会" → 自动创建日历事件
- "帮我把周五的会议推迟到下周一" → 修改日历
- 飞书消息提到"明天开会" → 自动提醒并建议创建日历

**接入方式**：Google Calendar MCP connector（claude.ai 已内置，需 OAuth 认证）

**优先级**：P0 — 价值极高，接入成本最低

#### 场景六：健康顾问

**目标**：整合健康和运动数据，主动提醒和建议。

**用户故事**：
- "这周运动量怎么样" → 分析步数、运动时长趋势
- 久坐超过2小时 → 主动提醒"该起来活动了"
- 睡眠数据异常 → "最近连续3天睡不到6小时，建议调整作息"
- 每周健康周报推送

**接入方式**：
- Apple Health / 华为健康数据导出为 CSV/JSON → MCP 解析
- 微信运动步数（通过微信 MCP 读取）
- 智能手环数据同步

**优先级**：P1 — 高价值，但需要手机端配合导出数据

#### 场景七：通知聚合中心

**目标**：所有 App 通知统一收口，AI 过滤噪音后只推送重要的。

**用户故事**：
- 手机上 50 条 App 通知 → 助手筛选后只给你 3 条重要的
- "外卖到了" → 推送
- "XX App 有新版本" → 忽略
- 快递状态更新 → "你有2个包裹明天到"

**接入方式**：
- Android：Tasker / MacroDroid → 通知转发到飞书/微信
- iOS：快捷指令 → 转发到 iMessage
- 或通过 KDE Connect 将手机通知同步到电脑

**优先级**：P1 — 高价值，但需要手机端配置

#### 场景八：财务管家

**目标**：自动分析消费，提醒预算。

**用户故事**：
- "这个月花了多少" → 分析支付宝/微信账单
- "和上个月比怎么样" → 消费趋势对比
- 月底超预算 → 主动提醒

**接入方式**：
- 微信/支付宝账单导出 CSV → MCP 解析
- 银行 App 账单截图 → Claude 视觉分析

**优先级**：P2 — 有价值，但数据获取依赖手动导出

#### 场景九：资讯过滤器

**目标**：不被信息流轰炸，只看值得看的。

**用户故事**：
- 订阅 RSS 源 → AI 按兴趣筛选，每天推送 5 条精选
- "最近AI领域有什么大事" → 从多个源综合分析
- 屏蔽标题党和重复报道

**接入方式**：
- RSS 订阅 + MCP 解析
- 微信公众号文章提取（通过微信 MCP）
- WebSearch 补充

**优先级**：P2 — 有价值，但需要定义兴趣模型

#### 场景十：剪贴板同步

**目标**：电脑和手机之间无缝传递文本和链接。

**用户故事**：
- 电脑上复制了一段文字，手机问"刚才复制了什么" → 返回内容
- 手机上看到一个链接，发给助手 → 助手在电脑上打开
- "把这段话复制到我的剪贴板" → 写入系统剪贴板

**接入方式**：自建 Clipboard MCP Server（Python，10 行代码）

**优先级**：P0 — 简单且实用

---

## Skill 清单

| Skill | 状态 | 默认模型 | 说明 |
|-------|------|----------|------|
| `inbox-triage` | ✅ 已安装 | DeepSeek | 消息分类与优先级 |
| `file-ops` | ✅ 已安装 | DeepSeek | 文件查找/读取/操作 |
| `dev-proxy` | ✅ 已安装 | 按复杂度路由 | 远程开发任务 |
| `deep-research` | ✅ 已安装 | Claude | 深度研究引擎 |
| `claude-cli` | ✅ 已安装 | Claude (via CLI) | 复杂任务路由到本地 Claude Max |
| `wechat-search` | ✅ 已安装 | Claude (via CLI) | 微信历史搜索（Claude CLI + MCP） |
| `daily-digest` | ✅ 已安装 | DeepSeek | 每日消息摘要（支持 cron 定时推送） |
| `memory` | ✅ 已安装 | DeepSeek | 跨会话持久记忆（文件存储） |
| `calendar` | 待开发 | DeepSeek | 日程管理（Google Calendar MCP） |
| `health` | 待开发 | Claude | 健康数据分析和建议 |
| `clipboard` | ✅ 已安装 | DeepSeek | 跨设备剪贴板同步 |

---

## 已知限制与风险

| 风险 | 影响 | 缓解方案 |
|------|------|----------|
| DeepSeek 无法可靠调用 CLI | 复杂路由失败 | 用 OpenClaw 原生 cliBackends + @model 切换 |
| 微信 MCP 依赖 PC 在线 | 电脑关机时无法读取微信数据 | 短期接受；长期考虑服务器部署 |
| 微信仅支持1对1 | 无法处理群聊消息 | 平台限制，暂不解决 |
| Claude OAuth token 过期 | CLI backend 静默失败 | 日常使用 Claude Code 保持 token 刷新 |
| 健康数据需手动导出 | 不能实时分析 | 探索自动同步方案 |
| 手机通知转发需配置 | 初始设置复杂 | 写一键配置脚本 |

---

## 实施路线

### Phase 1-3：基础系统 ✅ 已完成

消息流转、双模型路由、跨渠道转发、每日摘要、持久记忆、微信 MCP 全链路打通。

### Phase 4：日程 + 剪贴板（下一步）

| 任务 | 说明 | 优先级 |
|------|------|--------|
| Google Calendar MCP | 本地 MCP Server + Google OAuth | ✅ 完成 |
| calendar skill | 日程查询、创建、修改、搜索 | ✅ 完成 |
| Clipboard MCP Server | Python 自建，读写系统剪贴板 | ✅ 完成 |
| 晨间简报 | CLAUDE.md 规则整合日历+消息，用户说"早安"触发 | ✅ 完成 |
| Gmail MCP Server | 本地 MCP + OAuth，复用 huolalamap 项目 | ✅ 完成 |

### Phase 5：健康 + 通知

| 任务 | 说明 | 优先级 |
|------|------|--------|
| Garmin Connect MCP | 步数/心率/睡眠/运动/压力数据 | ✅ 完成 |
| health skill | 运动分析、久坐提醒、睡眠报告 | 待创建 |
| 手机通知转发 | Tasker/快捷指令 → OpenClaw | P1 |
| 通知过滤 skill | AI 过滤噪音通知 | P2 |

### Phase 6：财务 + 资讯

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 账单分析 | 微信/支付宝账单导出 + MCP 解析 | P2 |
| 快递追踪 | 快递100 API MCP | P2 |
| RSS 资讯过滤 | 订阅源 + AI 筛选 + 每日精选 | P2 |

---

## 里程碑记录

| 日期 | 事件 |
|------|------|
| 2026-03 早期 | 飞书 + iMessage 接通，OpenClaw agent 基本可用 |
| 2026-03-27 | **微信 MCP Server 打通** — 全量聊天/联系人/图片读取能力就绪 |
| 2026-03-27 | BlueBubbles webhook 404 修复 |
| 2026-03-27 | 需求文档 v2，确认架构方向（OpenClaw + DeepSeek + Claude CLI） |
| 2026-03-28 | Phase 1 完成：Claude CLI backend + @model 切换验证通过 |
| 2026-03-28 | Phase 2 完成：消息分类、跨渠道转发、daily-digest、MCP 权限预授权 |
| 2026-03-28 | Phase 3 完成：复杂度检测、memory skill、cron 定时任务、邮箱调研 |
| 2026-03-28 | 项目正式命名**头号助手**，扩展为全方位个人 AI 助手 |
| 2026-03-28 | Phase 4 完成：Google Calendar + Clipboard + Gmail MCP + 晨间简报 |
| 2026-03-28 | Phase 5 进行中：Garmin Connect MCP 接通（步数/心率/睡眠/运动） |
