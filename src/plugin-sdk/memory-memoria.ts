// Narrow plugin-sdk surface for the bundled memory-memoria plugin.
// Keep this list additive and scoped to symbols used under extensions/memory-memoria.

export type { OpenClawPluginApi } from "../plugins/types.js";
